# Human Consumption/Waste Comparator

Starter scaffold for a React + TypeScript app that compares a human figure to estimated lifetime/annual consumption or waste categories.

## Start

1. Install dependencies:
   npm install
2. Run development server:
   npm run dev

## Notes

- Current model values are placeholders for prototyping.
- Next step is replacing placeholder rates with source-backed values and citations.
